#!/bin/sh

#export TARGET_OUTPUT="target"

#if [ ! -d "$TARGET_OUTPUT" ]; then
#    echo "Control will enter here if $TARGET_OUTPUT doesn't exist. Therefore build operation going to start."
    echo "Maven clean build operation is starting."
    mvn clean install -DskipTests
#fi

java -jar target/{{artifactId}}-{{artifactVersion}}-fat.jar -conf config.json -Dhikaricp.configurationFile=/hikari.properties